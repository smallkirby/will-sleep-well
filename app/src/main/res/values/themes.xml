<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SleepingKirby" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/skblack_dark</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/skwhite_dark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/skblack_dark</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/skwhite_light</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/skblack_dark</item>

        <!-- Default colors -->
        <item name="android:textColor">@color/skwhite</item>
        <item name="android:itemBackground">@color/skblack_dark</item>

        <item name="android:windowBackground">@color/skblack</item>
        <item name="android:navigationBarColor">@color/skblack_dark</item>
    </style>

    <style name="Theme.SleepingKirby.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.SleepingKirby.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.SleepingKirby.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>